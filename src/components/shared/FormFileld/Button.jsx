import React from "react";

function Button(props) {
  <Button className={props.className}>{props.label}</Button>;
}

// Button.propTypes = {
//   name: PropTypes.stirngs,
// };

export default Button;
